package dk.dtu.compute.se.pisd.roborally.rest;

// Define the Action interface
public interface Action {
    // We could define some common methods here if there were any shared behaviors or properties
}
